.streamlit/secrets.toml
__pycache__/
*.pyc
.venv/
venv/
sheets_xlsx/
uploads/
.DS_Store

# 자격증명·사내문서 — 저장소에 올리지 않는다
우편번호 승인키.txt
*.docx
*.xlsx

# 어떤 형태로든 키가 저장소에 들어가지 않게 한다
.env
.env.*
*.key

# 실제 고객 개인정보 — 주소 이미지와 파싱 결과는 어떤 형태로도 올리지 않는다
out/
주소이미지/
이미지넣기/
address_images/
*.jsonl
